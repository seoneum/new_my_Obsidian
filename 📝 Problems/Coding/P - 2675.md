---
type: problem
title: P - 2675
created: 2026-01-27
updated: 2026-01-27T13:37:45
problem_type: coding
source: baekjoon
difficulty: easy
language: cpp
status: "[[🚜In Progress]]"
solved: false
tags:
  - problem
  - problem/coding
  - difficulty/easy
  - source/baekjoon
---
# 2675

> **coding** | 난이도: **easy** | 출처: baekjoon 2675

---

## 📋 문제

### 입력
```
전체 반복 횟수t, 글자 반복할 개수 n, 문자열 s
```

### 출력
```
문자열에서 각 글자를 n번 반복
```

### 제한
- 시간: 
- 메모리: 

---

## 🧠 접근

### 첫 생각
- 이거 이중for문 써야할 것 같은데? 근데 그럼 오래걸리는데
- 횟수 받고, 반복 받고, 문자 받은 다음에 문자열 std::views::iota해서 s\[i]로 슬라이스 쳐서 반복 박아야하나?
### 핵심 아이디어
- 하지만 더 좋은 방법이 있었습니다~
- Pilpeline을 활용하는 것인데요
- `auto view = s | std::views::transform([n](char c){return string(n,c);}) | std::views::join;`
- 여기까지인데요 상세 설명 들어가겠습니다.
- 먼저 s랑 같은 string views를 하나 만드는데 ramda함수를 씀
1) 외부 변수 n을 가져오고 s를 가져올 매개변수 char c를 하나 만듦.
2) `std::string(개수, 문자)` : 문자 s를 n번 반복해서 만들겠다.
3) `std::join` : \[\[AAA], \[BBB]] - >\[A,A,A,B,B,B]로 만듦.
### 필요 개념
- [[ ]]

---

## ✏️ 풀이

### C++
```cpp
#include <iostream>
#include <ranges>
#include <string>

auto main() -> int {
  std::cin.tie(nullptr)->sync_with_stdio(false);

  std::string s;
  int n, t;
  if (std::cin >> t) {
    while (t--) {
      if (std::cin >> n) {
        if (std::cin >> s) {
          auto re =
              s |
              std::views::transform([n](char c) { return std::string(n, c); }) |
              std::views::join;
          for (char c : re) {
            std::cout << c;
          }
          std::cout << "\n";
        }
      }
    }
  }
  return 0;
}
```

### 복잡도
- 시간: O()
- 공간: O()

---

## 🔍 복기

### 맞았으면
- 핵심: pilpeline을 사용해서 복잡도를 줄인 것.
- 더 좋은 방법: 이게 최선이긴 해

### 틀렸으면
- 실수: for문 2번 쓰면 너 해고야
- 정답: pilpeline과 views를 잘 알아둬야해요

---

## 📝 FC
#flashcards/coding

2675 핵심:: 

