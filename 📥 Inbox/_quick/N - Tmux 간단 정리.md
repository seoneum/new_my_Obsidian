---
migrated_from: CMDS/500. setting/501. Template/Master Template.md
updated: 2026-01-18T16:42:53
domain:
  - robotics
cmds: connect
---
# N - Tmux 간단 정리

## Notes
- tmux에 대한 간단한 명령어와 현재 내 설정
- 지금까지 논의한 **LazyVim 사용자 및 개발자를 위한 Tmux 최적화 설정**과 **필수 사용법**을 정리해 드립니다.

이 가이드는 **"실수 방지(세션 복구)"** 와 **"이동 효율성(Vim 통합)"** 에 초점을 맞췄습니다.

---

# 🚀 Tmux 개발자 환경 구축 가이드

## 1. 사전 준비 (TPM 설치)

플러그인을 관리해 주는 매니저(TPM)를 먼저 설치해야 모든 기능이 작동합니다. 터미널에 입력하세요.

Bash

```
git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
```

---

## 2. 설정 파일 (`~/.tmux.conf`)

아래 내용을 복사해서 `~/.tmux.conf` 파일에 덮어씌우세요.

_(순서 오류를 수정한 최종본입니다.)_

코드 스니펫

```
# ==========================================
# 1. 기본 동작 설정
# ==========================================
set -g default-shell /usr/bin/zsh    # 기본 쉘 Zsh
set -g mouse on                      # 마우스 사용 허용
set -g history-limit 50000           # 히스토리 5만 줄
set -s escape-time 0                 # Neovim 딜레이 제거 (필수)

# ==========================================
# 2. 인덱스 및 편의성 (1부터 시작)
# ==========================================
set -g base-index 1                  # 창 번호 1부터
set -g pane-base-index 1             # 패널 번호 1부터
set-window-option -g pane-base-index 1
set -g renumber-windows on           # 창 닫으면 번호 자동 정렬

# ==========================================
# 3. 키 바인딩 (직관적으로 변경)
# ==========================================
# 화면 분할 (현재 경로 유지)
bind | split-window -h -c "#{pane_current_path}"  # 좌우 분할
bind - split-window -v -c "#{pane_current_path}"  # 상하 분할
bind c new-window -c "#{pane_current_path}"       # 새 창

# 설정 리로드 (Prefix + r)
bind r source-file ~/.tmux.conf \; display "Config reloaded!"

# ==========================================
# 4. Vim 모드 (복사 모드)
# ==========================================
set-window-option -g mode-keys vi
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

# ==========================================
# 5. 플러그인 리스트
# ==========================================
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-yank'         # 시스템 클립보드 복사
set -g @plugin 'christoomey/vim-tmux-navigator' # Vim<->Tmux 이동 통합
set -g @plugin 'catppuccin/tmux'                # 테마
set -g @catppuccin_flavour 'mocha'

# --- 세션 자동 저장/복구 (순서 중요) ---
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

set -g @continuum-restore 'on'              # Tmux 켜면 자동 복구
set -g @resurrect-strategy-vim 'session'    # Vim 세션 복구
set -g @resurrect-capture-pane-contents 'on' # 패널 텍스트 복구

# ==========================================
# 6. TPM 실행 (반드시 파일 맨 끝에!)
# ==========================================
run '~/.tmux/plugins/tpm/tpm'
```

---

## 3. 적용 방법 (필수!)

설정 파일을 저장한 후, **최초 1회** 반드시 아래 단계를 수행해야 플러그인이 설치됩니다.

1. **Tmux 실행** (이미 실행 중이면 유지)
    
2. **플러그인 설치 키 입력:**
    
    - `Prefix(Ctrl+b)` 누른 뒤 떼고 $\rightarrow$ **대문자 `I`** (`Shift` + `i`)
        
3. 화면에 "TMUX ENVIRONMENT RELOADED"가 뜨면 완료.
    

---

## 4. 핵심 단축키 치트시트

기본 Prefix는 **`Ctrl + b`** 입니다.

### 🎮 이동 (Vim 통합)

LazyVim과 Tmux 패널 사이를 **Prefix 없이** 자유롭게 이동합니다.

- `Ctrl` + `h` : 왼쪽으로 이동
    
- `Ctrl` + `j` : 아래로 이동
    
- `Ctrl` + `k` : 위로 이동
    
- `Ctrl` + `l` : 오른쪽으로 이동
    

### 🪟 화면 분할 및 관리

- `Prefix` + **`|`** : 좌우 분할 (Shift + \)
    
- `Prefix` + **`-`** : 상하 분할
    
- `Prefix` + **`x`** : 현재 패널 닫기
    
- `Prefix` + **`z`** : 현재 패널 전체 화면 (Zoom) / 다시 누르면 복귀
    

### 💾 세션 저장 및 복구 (생명줄)

- **자동:** 15분마다 자동 저장됨. 재부팅 후 `tmux` 치면 자동 복구.
    
- `Prefix` + `Ctrl` + **`s`** : 수동 **저장** (Save)
    
- `Prefix` + `Ctrl` + **`r`** : 수동 **복구** (Restore)
    

### 📝 복사 모드 (Vim 스타일)

1. `Prefix` + **`[`** : 복사 모드 진입
    
2. `v` : 블록 지정 시작 (Visual mode)
    
3. `y` : 복사 (Yank) $\rightarrow$ 시스템 클립보드에도 저장됨
    
4. `q` : 모드 빠져나오기
    

---

이제 이 설정 하나면 **LazyVim 개발 환경**과 **Tmux**가 물 흐르듯 연동되고, 실수로 창을 닫아도 안전하게 복구할 수 있습니다.

**지금 바로 터미널에서 `Prefix + I`를 눌러 설치를 완료해 주세요!**

## Next
- [ ] 


