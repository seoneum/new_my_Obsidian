type: daily
title: "2025-12-22"
created: 2025-12-22
updated: "2025-12-22T16:24:41"
author: "[[김선음]]"
group: General
status: "[[🚜In Progress]]"
tags:
  - daily
aliases: []
---

# 2025-12-22

## Summary (Auto)
```dataviewjs
const today = dv.date("2025-12-22");
const yday = dv.date("2025-12-21");

const sameDay = (d, target) => d && target && d.toFormat("yyyy-MM-dd") === target.toFormat("yyyy-MM-dd");

// created 기반 (리눅스에서도 안정적)
const pagesToday = dv.pages()
  .where(p => p.created && sameDay(dv.date(p.created), today) && p.file.path !== dv.current().file.path);

const pagesYday = dv.pages()
  .where(p => p.created && sameDay(dv.date(p.created), yday) && p.file.path !== dv.current().file.path);

// tagging/needed 작업큐 카운트
const needTagging = dv.pages().where(p => Array.isArray(p.tags) && p.tags.includes("tagging/needed"));

dv.paragraph(`- Notes created today: **${pagesToday.length}**`);
dv.paragraph(`- Notes created yesterday: **${pagesYday.length}**`);
dv.paragraph(`- tagging/needed remaining: **${needTagging.length}**`);
````

## Summary (Write)

- Today in 1 line:
- Top priority:
- Biggest win:
- Biggest gap:
- Tomorrow focus:

---

## Plan (하루 설계)

### Top 3 (Must)

- [ ]
- [ ]
- [ ]

### Time blocks

- AM:
- PM:
- Night:

### If-Then (무너질 때 대비)

- If (상황): Then (대응):
- If (상황): Then (대응):

---

## TODO (Today)

- [ ]

### Open tasks (Vault)

```dataview
TASK
FROM ""
WHERE !completed
SORT file.mtime desc
LIMIT 40
```

---

## Note-taking (Capture)

### Quick log

- 2025-12-22 -

---

## Today I created

```dataview
LIST
FROM ""
WHERE created AND date(created) = date("2025-12-22")
AND file.path != this.file.path
SORT file.mtime desc
```

S## Yesterday I created

```dataview
LIST
FROM ""
WHERE created AND date(created) = date("2025-12-21")
AND file.path != this.file.path
SORT file.mtime desc
```

---

## Review (EOD)

### What happened

### What I learned (1~3 bullets)

### What to improve (1 thing)

### Next actions (tomorrow seed)

- [ ]